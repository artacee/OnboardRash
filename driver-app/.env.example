# ─── OnboardRash Driver App ─────────────────────────────
# Copy this file to .env and fill in your backend server IP.
# This value is baked into the JS bundle at build/export time.

# Backend API URL (Flask server)
# Replace with your server's LAN IP when building for a real device.
EXPO_PUBLIC_API_URL=http://192.168.1.100:5000
